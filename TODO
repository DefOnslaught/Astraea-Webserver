Frontend:
    About Page
        - Will need to make the 'Version History' better once there's more history, will need scrollbar

General Needs:

    Docker Support
        - Currently not tested, need to test and validate
        - Maybe use the docker sock to get system info for the administration server maintenance page