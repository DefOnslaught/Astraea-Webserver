Backend:
    - Add a method to generate csv files

    - Add Rate Limits

    - Ensure we properly handle the situation if an agent sends data with an unknown UUID or duplicate UUID 

Frontend:

    - About Page (opened via clicking the version in sidebar)
        - check for updates (front-end only, react goes out and checks version from git?)
        - Version history (no backend, just text on front-end)
        - About section

General Needs:

    Administration - System Access (Rename to something else) 
        - Have a button to refresh cache manually (if for some reason the data showing is wrong)


    Notification
        - Add: Notify on Server delete, Add, modify


    Make it so user settings for the site are saved in the database - Only send the settings to update when the user signs out or after some time to avoid API hits
        - User signs in, when we are making their auth cookies, we also make ones for the site
        - Things to Save: 
            - Sidebar closed/open
            - Light mode/Dark Mode (A lot of work)
            

    For the inspect server section
        - Add a search bar to 'Patch History'


    setup.sh
        Install the proper node versions, use this site as a guide: https://nodesource.com/products/distributions

    Update.sh
        - Will not work with the docker deployment until we figure that out